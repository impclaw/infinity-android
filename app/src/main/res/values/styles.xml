<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme.Base" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colourPrimary</item>
        <item name="colorPrimaryDark">@color/colourPrimaryDark</item>
        <item name="colorAccent">@color/colourAccent</item>
        
    </style>

    <style name="AppTheme.Toolbar" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--<item name="android:textColorPrimary">@color/colourAccent</item>-->
    </style>

    <style name="AppTheme.Popup" parent="@style/ThemeOverlay.AppCompat.Dark">
        <!--<item name="android:background">@color/colourAccent</item>-->
        <!--<item name="android:textColor">@color/colourPrimary</item>-->
    </style>

    <style name="AppTheme.CardView" parent="CardView.Dark">
    </style>

    <style name="AppTheme.Stats" parent="AppTheme.Base">
        <item name="android:layout_gravity">center</item>
        <item name="android:text">10</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>

    </style>


</resources>
